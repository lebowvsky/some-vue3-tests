import InputTextParticle from "./InputTextParticle.vue";
import { describe, it, expect } from "vitest";

describe("InputTextParticle", () => {
  it("should be good", () => {
    expect(true).toBe(true);
  });
});
